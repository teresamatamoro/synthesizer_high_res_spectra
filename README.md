# synthesizer_high_res_spectra

data_loader.py contains the CAMELS loaders with the BH components incorporated.

The .yaml files have the cloudy parameters varied to create the high resolution grids.

plot_spectra.ipynb contains an example of the error gotten when trying to run the high res grids.
